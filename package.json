{
  "name": "teleparty-booster",
  "version": "1.0.0",
  "description": "emoji and gif support for teleparty",
  "scripts": {
    "build:app": "esbuild src/index.ts --bundle --outfile=dist/app.js",
    "build:watcher": "esbuild src/build/reload-handler.ts --bundle --outfile=reload-handler.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@bscotch/debounce-watch": "^0.5.1",
    "@types/node": "^17.0.21",
    "@types/ws": "^8.5.3",
    "esbuild": "^0.14.27",
    "typescript": "^4.6.2",
    "ws": "^8.5.0"
  }
}
